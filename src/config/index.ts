export const HttpConfig = {
  BASEURL: "https://example.com",
  TIMEOUT: 60000,
};

export const AppConfig = {
  PORT: 8081,
};

export const CodeMap = {
  repeat: 100000, // 名称重复
  params: 100001, // 参数错误
  exists: 100002, // 记录不存在
  secure: 100003, // 内容审核不通过
};
